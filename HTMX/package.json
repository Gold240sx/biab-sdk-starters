{
	"name": "biab-htmx",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"engines": {
		"bun": ">=1.1.0"
	},
	"scripts": {
		"dev": "bun --hot run server.ts",
		"start": "bun run server.ts",
		"check": "tsc --noEmit"
	},
	"dependencies": {
		"@biab-dev/sdk": "^0.9.2"
	},
	"devDependencies": {
		"@types/bun": "^1.1.0",
		"typescript": "^5.5.0"
	}
}
